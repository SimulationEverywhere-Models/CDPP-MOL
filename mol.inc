#BeginMacro(N)
20
#EndMacro

#BeginMacro(M)
200
#EndMacro

% (xf - x0) / (n - 1)
#BeginMacro(DX)
( (1 - 0) / (20 - 1) )
#EndMacro

% (tf - t0) / (m - 1)
#BeginMacro(DT)
( (0.25 - 0) / (200 - 1) )
#EndMacro